keep updating the code_map markdown file, use it as reference only refrence code if needed.

also i have seen you running the build command multiple times, dont do it, it dosent work in the terminal i will do it manually