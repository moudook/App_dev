# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ==========================================
# JVM Memory Optimization for Build Speed
# ==========================================
# Increased for large Kotlin/Compose projects
# Metaspace increased to handle KSP, Compose compiler, and large dependency graphs
org.gradle.jvmargs=-Xmx2g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:MaxMetaspaceSize=512m -XX:+UseParallelGC

# Use Android Studio's bundled JDK (local development only)
# CI/Docker: This is ignored as the JDK is set in the Docker image
# org.gradle.java.home - Let Docker use its own JDK

# Enable parallel execution of independent modules
org.gradle.parallel=true

# Enable build cache for faster rebuilds
org.gradle.caching=true

# Enable configuration cache (safe for server-only builds)
# Disabled by default - enable in CI with: --configuration-cache
# org.gradle.configuration-cache=true

# Enable file system watching for faster incremental builds
org.gradle.vfs.watch=true

# ==========================================
# AndroidX Configuration
# ==========================================
android.useAndroidX=true
# Disable Jetifier (assuming full AndroidX migration)
android.enableJetifier=false

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official


# Enables namespacing of each library's R class
android.nonTransitiveRClass=true

# ==========================================
# KSP (Kotlin Symbol Processing) Configuration
# ==========================================
# Enable incremental processing for KSP
ksp.incremental=true
ksp.incremental.intermodule=true

# Suppress compileSdk warning
android.suppressUnsupportedCompileSdk=35

# Disable classpath checking (causes serialization issues with large dependency maps)
android.enableClasspathSnapshotVerification=false

